server:
  error:
    include-stacktrace: on_param
    include-message: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/processors?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG